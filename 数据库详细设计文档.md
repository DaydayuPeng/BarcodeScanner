# 兔咕噜网站 - 数据库详细设计文档

> **数据库名称**：`tugulu_warehouse`  
> **存储引擎**：InnoDB  
> **字符集**：UTF-8mb4（支持特殊字符与emoji）  
> **版本**：V1.0

---

## 1. 员工/管理员表 (`sys_user`)
系统登录账号及权限控制。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `username` | VARCHAR(50) | NO | - | 登录账号（唯一） |
| `password` | VARCHAR(255) | NO | - | BCrypt加密密码 |
| `real_name` | VARCHAR(50) | NO | - | 真实姓名（用于记录操作人） |
| `role` | VARCHAR(20) | NO | 'employee' | 角色：admin / employee |
| `status` | TINYINT(1) | NO | 1 | 账号状态：1启用 0禁用 |
| `create_time` | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| `update_time` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引策略**：
- 主键：`id`
- 唯一索引：`uk_username` (`username`)

---

## 2. 前台CMS内容表 (`cms_content`)
存储公司概况、服务介绍、产品介绍的图文内容（中英文）。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `type` | VARCHAR(20) | NO | - | 类型：company（公司概况）, service（服务介绍）, product（产品介绍） |
| `title_en` | VARCHAR(200) | YES | NULL | 英文标题 |
| `content_en` | LONGTEXT | YES | NULL | 英文正文（前台展示） |
| `content_zh` | LONGTEXT | YES | NULL | 中文正文（后台编辑预览） |
| `images` | TEXT | YES | NULL | 图片URL列表（JSON数组格式） |
| `sort_order` | INT(11) | YES | 0 | 排序权重（数值越大越靠前） |
| `update_by` | VARCHAR(50) | YES | NULL | 最后编辑人 |
| `update_time` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引策略**：
- 主键：`id`
- 普通索引：`idx_type` (`type`)

---

## 3. 快递入库记录表 (`inbound_order`)
核心数据表，用于存储扫描入库的包裹信息。数据保留12个月。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `tracking_no` | VARCHAR(100) | NO | - | 快递单号（核心查询字段） |
| `shelf_no` | VARCHAR(50) | YES | NULL | 货架号（入库时选填） |
| `image_url` | VARCHAR(500) | YES | NULL | 入库拍摄图片存储URL |
| `inbound_time` | DATETIME | NO | CURRENT_TIMESTAMP | 入库时间 |
| `create_by` | VARCHAR(50) | YES | NULL | 入库操作人 |
| `status` | TINYINT(1) | NO | 1 | 状态：1已入库（查询显示），0已删除（逻辑删除/待清理） |

**索引策略**：
- 主键：`id`
- 普通索引：`idx_tracking_no` (`tracking_no`) —— **重要**：用于单号精确查询和`IN`批量查询。
- 普通索引：`idx_inbound_time` (`inbound_time`) —— 用于12个月数据清理定时任务。

> **数据清理说明**：`status=0`且`inbound_time`超过12个月的记录，将由定时任务每日凌晨执行物理删除（`DELETE`），释放磁盘空间。

---

## 4. 工单主表 (`work_order`)
最复杂的业务核心，管理订单从备货到完成的全生命周期。**必须使用乐观锁**。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID（工单号） |
| `customer_id` | VARCHAR(50) | NO | - | 客户编号（必填） |
| `batch_no` | VARCHAR(100) | NO | - | 批次号（必填） |
| `status` | VARCHAR(20) | NO | 'PENDING_PREP' | 状态枚举（见下方状态表） |
| `total_quantity` | INT(11) | NO | 0 | 当前总件数 |
| `weight` | DECIMAL(10,2) | YES | NULL | 总重量（KG），待封箱时必填 |
| `volume` | DECIMAL(10,2) | YES | NULL | 总体积（m3），待封箱时必填 |
| `packer_id` | BIGINT(20) | YES | NULL | 打包人ID（关联`sys_user.id`，点击待打包时记录） |
| `create_by` | BIGINT(20) | NO | - | 工单创建人ID |
| `created_at` | DATETIME | NO | CURRENT_TIMESTAMP | 创建时间 |
| `updated_at` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| `version` | INT(11) | NO | 0 | **乐观锁版本号**（并发控制必用） |

**工单状态枚举 (`status`)**：

| 状态码 | 显示名称 | 备注 |
| :--- | :--- | :--- |
| `PENDING_PREP` | 待备货 | 初始创建状态 |
| `PENDING_PACK` | 待打包 | 备货确认后 |
| `PACKED` | 已打包 | 打包确认后，支持补货 |
| `PENDING_SEAL` | 待封箱 | 封箱操作后 |
| `PENDING_SHIP` | 待发货 | 封箱确认后 |
| `COMPLETED` | 已完成 | 最终确认后（移出当前列表） |

**索引策略**：
- 主键：`id`
- 普通索引：`idx_customer_id` (`customer_id`) —— 用于按客户统计。
- 普通索引：`idx_packer_id` (`packer_id`) —— 用于按打包人统计重量。
- 普通索引：`idx_status` (`status`) —— 用于前台按Tab加载不同状态的列表。

---

## 5. 工单操作日志表 (`work_order_log`)
记录每一步操作，满足“工单流转记录”需求，由AOP切面自动插入。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `order_id` | BIGINT(20) | NO | - | 关联工单ID |
| `operator_id` | BIGINT(20) | NO | - | 操作人ID |
| `from_status` | VARCHAR(20) | YES | NULL | 操作前状态 |
| `to_status` | VARCHAR(20) | NO | - | 操作后状态 |
| `remark` | VARCHAR(500) | YES | NULL | 备注（如补货件数、操作说明） |
| `operate_time` | DATETIME | NO | CURRENT_TIMESTAMP | 操作时间 |

**索引策略**：
- 主键：`id`
- 普通索引：`idx_order_id` (`order_id`) —— 用于前台“查看历史流转记录”。

---

## 6. 每日入库统计表 (`stat_inbound`)
定时任务统计结果表，避免实时聚合导致性能问题。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `stat_date` | DATE | NO | - | 统计日期（唯一） |
| `total_count` | INT(11) | NO | 0 | 当日入库总件数 |
| `update_time` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引策略**：
- 主键：`id`
- 唯一索引：`uk_stat_date` (`stat_date`)

---

## 7. 每日打包重量统计表 (`stat_packing`)
按日期、员工、客户三个维度统计打包重量。

| 字段名 | 类型 | 允许空 | 默认值 | 注释 |
| :--- | :--- | :--- | :--- | :--- |
| `id` | BIGINT(20) | NO | AUTO_INCREMENT | 主键ID |
| `stat_date` | DATE | NO | - | 统计日期 |
| `packer_id` | BIGINT(20) | NO | - | 打包员工ID（关联`sys_user.id`） |
| `customer_id` | VARCHAR(50) | NO | - | 客户编号 |
| `total_weight` | DECIMAL(10,2) | NO | 0 | 该员工该客户当日打包总重量 |
| `update_time` | DATETIME | NO | CURRENT_TIMESTAMP ON UPDATE | 更新时间 |

**索引策略**：
- 主键：`id`
- 复合唯一索引：`uk_date_packer_customer` (`stat_date`, `packer_id`, `customer_id`) —— 确保按天+员工+客户维度唯一，便于累加更新。